Slide 36: 最後總結
投影片內容

本單元三個核心觀念：

推薦系統可以表示成矩陣問題。
SVD 可以找出矩陣中的主要潛在方向。
推薦分數可以透過 user embedding 與 item embedding 的內積計算。

補充：

SVD 是理解推薦系統的好入口；
真實系統通常會使用 weighted matrix factorization、WALS 或更複雜的模型來處理稀疏資料。

說明重點

最後可以用一句話收尾：

SVD 的價值不只是分解矩陣，而是讓我們用較低維的向量空間理解使用者與物品之間的隱藏關係。