{{溫和}} 接著把低秩想法，寫成一個明確的最佳化問題。假設我們先把沒有觀察到的互動都填成 0，原始矩陣叫 A。目標是找到兩個矩陣 U 和 V，讓 U 乘上 V 的轉置之後，盡量接近 A。數學式寫成最小化 A 減 UV 轉置的 Frobenius 範數平方。Frobenius 範數可以先想成，把每一格誤差都平方，再全部加起來。就像改作業時，不只看某一題錯多少，而是把整份作業的差距一起算。{{強調}} SVD 的角色，就是在固定 rank k 的條件下，給出最好的低秩近似，讓整體平方誤差最小。它不是隨便壓縮，而是有數學保證的壓縮。