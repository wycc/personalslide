{{溫和}} 剛剛說到，Σ 裡的奇異值代表縮放大小。接著要抓住一個重要觀念：奇異值通常會由大到小排列。越大的奇異值，代表它對原始矩陣的影響越大，也就是越重要的方向。SVD 也可以改寫成一串相加的形式：第一個模式，加上第二個模式，再加上後面的模式。每一項都是一個 rank-1 矩陣。rank-1 可以先想成一張很簡單的外積圖案，只有一種主要結構。把很多個簡單圖案一層一層疊起來，就能逐步重建原本的矩陣。{{強調}} 第一層通常抓到最大結構，後面補細節；越後面越可能只是小變化或雜訊。