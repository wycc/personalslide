{{溫和}} 前面提到加權目標函數，接著問題就是：要怎麼把 U 和 V 真的算出來？WALS 的全名是 Weighted Alternating Least Squares，也就是加權交替最小平方法。它的做法很像兩個人輪流整理房間。一次只讓其中一個人動，另一個先固定，事情就簡單很多。{{強調}} 如果同時求使用者向量 U 和物品向量 V，問題會比較難解。可是先固定 U，只求 V，問題就會變成最小平方法，也就是找一組數字，讓預測誤差平方和最小。接著再反過來，固定 V，求 U。兩個步驟一直交替，直到 loss 幾乎不再下降。這其實就是把訓練轉成線性代數問題。