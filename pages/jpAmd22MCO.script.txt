Speaker 1: 接著把 h(x)=θ0+θ1x1+θ2x2，整理成更像數學工具箱的寫法。關鍵是新增一個固定的虛擬特徵 x0，而且它永遠等於 1。
Speaker 2: {{恍然大悟}} 喔！所以 θ0 就可以寫成 θ0 乘以 x0，因為 x0 是 1，乘完還是 θ0。這有點像在表格最前面加一欄「固定加分」，每筆資料都一樣，不改變意思，但公式瞬間變整齊。
Speaker 1: 沒錯。於是假設函數可以寫成加總形式：h(x)=Σθj xj。以兩個原始特徵為例，j 從 0 加到 2，也就是 θ0x0、θ1x1、θ2x2 全部加起來。
Speaker 2: 圖上也把它畫成向量。參數向量 θ 放 θ0、θ1、θ2；特徵向量 x 放 x0、x1、x2。兩個向量配對相乘再加總，就得到預測值。
Speaker 1: 這就是 h(x)=θᵀx，念作 θ 轉置乘上 x。公式變短不是偷懶，而是更有系統，方便擴充到更多特徵。